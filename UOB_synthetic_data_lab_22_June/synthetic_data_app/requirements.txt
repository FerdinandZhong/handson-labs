# Core API
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
requests>=2.31.0,<3.0.0
pydantic>=2.0.0,<3.0.0
# Agentic mode — pinned to avoid pip resolution-too-deep on crewai's large dep tree
crewai==1.14.1
crewai-tools>=0.38.0,<0.45.0
# Impala connectivity (ImpalaQueryTool + pipeline scan)
impyla>=0.19.0
thrift-sasl>=0.4.3
